<Window x:Class="ParticleMaker.Dialogs.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" Title="{Binding Path=DialogTitle}" Name="this"
        WindowStartupLocation="CenterOwner" SizeToContent="WidthAndHeight"
        MinWidth="300" MaxWidth="350" MinHeight="200" ShowInTaskbar="False" WindowStyle="ToolWindow"
        DataContext="{Binding ElementName=this}"
        d:DataContext="{d:DesignData Source=/Dialogs/DesignData/InputDialogData.xaml}" ResizeMode="NoResize">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <!--INPUT DIALOG MESSAGE-->
        <TextBlock Text="{Binding Path=Message}" Grid.Row="0" Margin="5" TextWrapping="Wrap" VerticalAlignment="Bottom"/>

        <Grid Grid.Row="1" Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBox Name="InputTextBox" Grid.Row="0" Grid.ColumnSpan="2" MinWidth="250" Margin="5" VerticalAlignment="Center"
                     Text="{Binding Path=InputValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

            <StackPanel Orientation="Horizontal" Grid.Row="2" Grid.Column="1" VerticalAlignment="Bottom" HorizontalAlignment="Right">
                <Button Content="Ok" Margin="6" Width="75" Height="25"
                        Command="{Binding Path=OkCommand}" IsEnabled="{Binding ElementName=InputTextBox, Path=Text.Length}"/>
                <Button Content="Cancel" Margin="6" Width="75" Height="25" HorizontalAlignment="Right"
                        Command="{Binding Path=CancelCommand}"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>