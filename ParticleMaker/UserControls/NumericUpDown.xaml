<UserControl x:Class="ParticleMaker.UserControls.NumericUpDown"
             xmlns:this="clr-namespace:ParticleMaker.UserControls"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ParticleMaker.UserControls"
             mc:Ignorable="d" Height="30" Width="61.191" MinHeight="30" MaxHeight="30">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!--Holds the value of the up down control-->
        <TextBox Name="ValueTextbox" Grid.Column="0" Grid.Row="0" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"
                 PreviewKeyDown="TextBox_PreviewKeyDown"
                 Text="{Binding Path=Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
        </TextBox>

        <Grid Grid.Column="1" Grid.Row="0" Margin="5, 0, 0, 0">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>

            <!--Value Up Button-->
            <Canvas Width="25" Height="25" Grid.Row="0" Margin="0, 0, 0, 0.5">
                <Polygon Name="UpArrow"
                         Points="12.5,0.5 24.5,11 0.5,11"
                         Stroke="Gray"
                         StrokeThickness="0.5" MouseUp="UpArrow_MouseUp">
                    <Polygon.Style>
                        <Style TargetType="{x:Type Polygon}">
                            <Setter Property="Fill" Value="Gray"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Fill" Value="LightGray"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Polygon.Style>
                </Polygon>
            </Canvas>

            <!--Value Down Button-->
            <Canvas Grid.Row="1" Width="25" MaxWidth="25" Height="25" Margin="0, 0.5, 0, 0">
                <Polygon Points="0.5,1 24.5,1 12.5,11.5"
                         Stroke="Gray"
                         StrokeThickness="0.5" MouseUp="Polygon_MouseUp">
                    <Polygon.Style>
                        <Style TargetType="{x:Type Polygon}">
                            <Setter Property="Fill" Value="Gray"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Fill" Value="LightGray"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Polygon.Style>
                </Polygon>
            </Canvas>
        </Grid>
    </Grid>
</UserControl>
